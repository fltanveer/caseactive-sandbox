{
  "buildCommand": "cd onboarding-app && npm install && npm run build",
  "outputDirectory": "onboarding-app/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
